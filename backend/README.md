# CampusConnect AI — Backend

Spring Boot 3 REST API (Java 17) for CampusConnect AI.

> This module is built iteratively. Setup and run instructions will be added once the Spring Boot project is bootstrapped.
