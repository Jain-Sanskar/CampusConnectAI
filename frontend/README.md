# CampusConnect AI — Frontend

React 18 + Vite + Tailwind CSS single-page application for CampusConnect AI.

> This module is built iteratively. Setup and run instructions will be added once the React project is scaffolded.
